{
  "name": "kypcar-exam-backend",
  "version": "1.0.0",
  "description": "Backend integration for the Kypcar technical exam.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
